{
  "name": "checkpoint",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5175",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:worker": "wrangler deploy --config worker/wrangler.toml"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "wrangler": "^4.129.1"
  }
}
